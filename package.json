{
  "private": true,
  "name": "traduki",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "author": "havelaer",
  "license": "MIT",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "yarn run build && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^15.0.2",
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
