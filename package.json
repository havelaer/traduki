{
  "private": true,
  "name": "traduki",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "author": "havelaer",
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "yarn run build && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^14.0.21",
    "lerna": "^3.22.1",
    "typescript": "^3.9.6"
  }
}
